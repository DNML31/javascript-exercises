const getTheTitles = (function(x) {
    let newArray = [];
    newArray.push(x[0].title);
    newArray.push(x[1].title);
    return newArray;
});

//There is an array that contains two objects (each with a title and author).
//Put each object through a function that extracts the titles and puts them into
//a new array.




// Do not edit below this line
module.exports = getTheTitles;
